1. create react+vite project.
2. run this command 
```
npm install @reduxjs/toolkit react-redux
```
its true that u can read state from any component by passing probs, but u can't read directly.

3. u can read a state from any component using redux.

4. make redux/store.js

5. import in main.jsx
```
import { Store } from './redux/store.js'
import { Provider } from 'react-redux'
```