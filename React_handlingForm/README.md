It very painful process to make a form using pure/venila react js
so we use "react-hook-form" library.

delay fun - to handle network delay
disabled submit button - so that it is not clicked multiple times and data is not submitted many times